# https://taskfile.dev
# docker run -v "D:\Traning golang\__practicum__\metriccollector\metriccollector:/app" gotests task static-tests
version: '3'

silent: true
tasks:
  unit-tests:
    cmds:
      - echo "- Run unit tests"
      - |      
        go build -ldflags '-s -w -X main.buildVersion=1.0.0 -X main.buildDate=2023-01-23 -X main.buildCommit=0c2fs'  -o cmd/agent/agent cmd/agent/main.go
        go build -ldflags '-s -w -X main.buildVersion=1.0.0 -X main.buildDate=2023-01-23 -X main.buildCommit=0c2fs'  -o cmd/server/server cmd/server/main.go
        # Run your test commands

        SERVER_PORT=8080
          ADDRESS="localhost:${SERVER_PORT}"
          TEMP_FILE=8080
          metricstestbeta -test.v -test.run=^TestIteration7$ \
            -agent-binary-path=cmd/agent/agent \
            -binary-path=cmd/server/server \
            -server-port=$SERVER_PORT \
            -source-path=.
  static-tests:
    cmds:
    - echo "- Run static tests"
    - go vet -vettool=statictest ./...
  run-all:
    deps: [run-server, run-agent]
  run-server:
    cmds:
    - echo "Run server"
    - go run cmd/server/main.go
  run-agent:
    cmds:
    - echo "Run agent"
    - go run cmd/agent/main.go
